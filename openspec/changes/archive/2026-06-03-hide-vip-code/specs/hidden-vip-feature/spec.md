## ADDED Requirements

### Requirement: Hiding the VIP code feature
The system SHALL NOT display the VIP code input and validation button to the user in the booking form.

#### Scenario: VIP section is invisible
- **WHEN** the user visits the booking form
- **THEN** the "Do you have a code?" section SHALL NOT be visible in the layout
