## 1. UI Implementation

- [x] 1.1 Update `SubmitBtn` label in `src/sections/form.jsx` to "Reserve Now" when `total` is 0.

## 2. Verification

- [x] 2.1 Verify that the button shows "Reserve Now" when the total amount is zero.
- [x] 2.2 Verify that the button still shows "Buy Now" when the total amount is greater than zero.
